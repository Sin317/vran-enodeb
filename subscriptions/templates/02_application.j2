apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: {{item.name_policy}}
  namespace: {{item.namespace}}
spec:
  componentKinds:
    - group: apps.open-cluster-management.io
      kind: Subscription
  descriptor: {}
  selector:
    matchExpressions:
      - key: app
        operator: In
        values:
          - c{{item.name_policy}}